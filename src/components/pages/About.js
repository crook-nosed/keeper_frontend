import React, { Fragment } from 'react'

const About = () => {
    return (
        <Fragment>
            <h1>About ...</h1>
            <p> A simple full stack with functionalities similar to google keep app built with react and django.</p>
        </Fragment>
    )
}

export default About
